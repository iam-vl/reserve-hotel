## Useful commands 

```
git config user.name "???"
git config yser.email "???"
touch main.go
go mod init github.com/???/???
go get github.com/gofiber/fiber/v2 
make build
make run
make test
./bin/api --listenAddr :1111
```